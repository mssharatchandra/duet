# Verifies the self-hosted observability stack actually comes up and accepts
# data — because local dev on this project's Mac has no Docker (no admin
# rights; DECISIONS.md 0007). CI runners have Docker, so the stack is proven
# here on every infra change: Langfuse ingests a trace, Grafana provisions all
# datasources, Alloy ships a structured event to Loki, and CallStore writes a row.

name: infra

on:
  push:
    branches: [main]
    paths: ["infra/**", "agent/duet_agent/*telemetry.py", "web-demo/server.py", ".github/workflows/infra.yml"]
  pull_request:
    paths: ["infra/**", "agent/duet_agent/*telemetry.py", "web-demo/server.py", ".github/workflows/infra.yml"]

jobs:
  stack-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Start Langfuse stack
        working-directory: infra
        run: |
          cp .env.example .env
          mkdir -p ../.local/telemetry
          touch ../.local/telemetry/ci.jsonl
          docker network create duet-backplane
          docker compose -p langfuse -f langfuse-compose.yml up -d --quiet-pull
      - name: Langfuse healthy
        run: |
          for _attempt in $(seq 1 60); do
            curl -sf http://localhost:3000/api/public/health && exit 0; sleep 5;
          done
          docker compose -p langfuse -f infra/langfuse-compose.yml logs --tail 50; exit 1
      - name: Langfuse accepts a trace (ingestion API, dev keys)
        run: |
          code=$(curl -s -o /tmp/resp -w '%{http_code}' -u pk-lf-duet-dev:sk-lf-duet-dev \
            -H 'Content-Type: application/json' \
            -d "{\"batch\":[{\"id\":\"$(uuidgen)\",\"type\":\"trace-create\",\"timestamp\":\"$(date -u +%FT%T.000Z)\",\"body\":{\"id\":\"$(uuidgen)\",\"name\":\"ci-smoke\"}}]}" \
            http://localhost:3000/api/public/ingestion)
          cat /tmp/resp; [ "$code" = "207" ]
      - name: Start metrics, logs and call database
        working-directory: infra
        run: docker compose -p duet-obs -f observability-compose.yml up -d --quiet-pull
      - name: Grafana provisioned with Duet datasources
        run: |
          grafana_password=$(sed -n 's/^GRAFANA_ADMIN_PASSWORD=//p' infra/.env | tail -1)
          for _attempt in $(seq 1 30); do curl -sf http://localhost:3001/api/health && break; sleep 3; done
          curl -sf -u "admin:$grafana_password" http://localhost:3001/api/datasources | grep -q '"uid":"duet-pg"'
          curl -sf -u "admin:$grafana_password" http://localhost:3001/api/datasources | grep -q '"uid":"duet-prom"'
          curl -sf -u "admin:$grafana_password" http://localhost:3001/api/datasources | grep -q '"uid":"duet-loki"'
      - name: Alloy ships structured session logs to Loki
        run: |
          for _attempt in $(seq 1 30); do curl -sf http://localhost:12345/-/ready && break; sleep 2; done
          curl -sf http://localhost:12345/-/ready
          printf '{"timestamp":"%s","session_id":"ci-smoke","trace_id":"ci-trace","type":"session","state":"started"}\n' "$(date -u +%FT%TZ)" >> .local/telemetry/ci.jsonl
          for _attempt in $(seq 1 30); do
            curl -sgG --data-urlencode 'query={job="duet-live"} |= "ci-smoke"' --data-urlencode 'since=5m' http://localhost:3100/loki/api/v1/query_range | grep -q 'ci-smoke' && exit 0
            sleep 2
          done
          docker compose -p duet-obs -f infra/observability-compose.yml logs --tail 80 alloy loki
          exit 1
      - name: CallStore writes a row to duet-postgres
        run: |
          pip install "psycopg[binary]" -q
          duet_pg_password=$(sed -n 's/^DUET_PG_PASSWORD=//p' infra/.env | tail -1)
          PYTHONPATH=agent DATABASE_URL="postgresql://duet:$duet_pg_password@localhost:5433/duet" python3 -c "
          from duet_agent.telemetry import CallStore
          ok = CallStore().insert({'call_id': 'ci-smoke', 'mode': 'ci', 'scenario': 'smoke',
                                   'duration_s': 1.0, 'takeover_rate': 0.0})
          assert ok, 'insert failed'
          print('postgres write OK')"
      - name: Failure diagnostics
        if: failure()
        run: |
          docker stats --no-stream || true
          docker compose -p langfuse -f infra/langfuse-compose.yml ps || true
          docker compose -p langfuse -f infra/langfuse-compose.yml logs --tail 120 || true
          docker compose -p duet-obs -f infra/observability-compose.yml ps || true
          docker compose -p duet-obs -f infra/observability-compose.yml logs --tail 120 || true
