# Secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
.next/
dist/

# Model weights & audio artifacts (large binaries stay out of git)
models/
*.safetensors
*.gguf
*.bin
*.pt
recordings/
*.wav
*.mp3
!eval/fixtures/**/*.wav

# Captured web-demo sessions (opt-in ASR eval recordings — real mic audio, stays local; see
# web-demo/capture.py and docs/DECISIONS.md 0012). *.wav above already covers the audio; the
# JSONL rows carry transcripts, so keep the whole directory out of git, not just the binaries.
eval/asr/sessions/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local data volumes
infra/data/
.local/
